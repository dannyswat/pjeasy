# apis folder

- It stores all API handlers by domain (e.g. auth_handler.go, project_handler.go).
- It stores start up routing and start up configuration for settup up an API server (api_server.go)
- It may contains API layer utility functions